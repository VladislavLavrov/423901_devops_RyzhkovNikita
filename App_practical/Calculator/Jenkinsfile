pipeline {
    agent any
    
    stages {
        stage('Hello') {
            steps {
                echo 'Hello jenkins'
            }
        }
		
        stage('Navigate to project directory'){
			steps{
				script {
                    if (isUnix()) {
                        sh 'cd App_practical/Calculator'
                    } else {
                        bat 'cd App_practical/Calculator'
                    }
                }
			}
		}
        stage('Build Docker Image') {
            steps {
                script {
                    if (isUnix()) {
                        sh 'ls -l'
                        sh 'docker compose build'
                    } else {
                        bat 'dir'
                        bat 'docker compose build'
                    }
                }
            }
        }
        
        stage('Start Docker Container') {
            steps {
                script {
                    if (isUnix()) {
                        sh 'docker compose up -d'
                    } else {
                        bat 'docker compose up -d'
                    }
                }
            }
        }
    }
}